/**
 * Domaine partagé : entités JPA, repositories, services métier, sécurité, config.
 *
 * <p>Ne pas placer de {@code @RestController} ici — utiliser {@code api.web},
 * {@code api.mobile} ou {@code api.shared}.
 */
package com.flysoft.fretcorridor.common;
